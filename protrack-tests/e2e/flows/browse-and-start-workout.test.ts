describe('Flow 2: Explorar e iniciar treino', () => {
  beforeEach(async () => {
    // Reset app state or navigate to Home
  });

  it('deve buscar exercício por nome e exibir resultados corretos', async () => {
    // 1. Digitar "Supino" no campo de busca
    // 2. Verificar se o card "Supino Reto" aparece
    // 3. Verificar se outros exercícios não relacionados sumiram
  });

  it('deve filtrar por grupo muscular corretamente', async () => {
    // 1. Abrir modal de filtros
    // 2. Selecionar "Peito"
    // 3. Aplicar filtro
    // 4. Verificar se todos os resultados são da categoria "Peito"
  });

  it('deve entrar no Modo Ativo ao tapar em "Iniciar treino"', async () => {
    // 1. Tap em um plano de atleta
    // 2. Tap em "Iniciar treino"
    // 3. Verificar se a tela de Modo Ativo (player + timer) é exibida
  });
});
